[common]
bind_addr = 0.0.0.0
bind_port = 7000
kcp_bind_port = 7000

# Dashboard
dashboard_addr = 0.0.0.0
dashboard_port = 7500
dashboard_user = admin
dashboard_pwd = admin

# Auth
authentication_method = token
token = frp-token

# Log
log_file = /var/log/frps.log
log_level = info
log_max_days = 3
